import type { NextPage } from 'next';
import Head from 'next/head';

import Layout from '@/components/Layout';

const Home: NextPage = () => {
  return <Layout title="Andromeda">...</Layout>;
};

export default Home;
